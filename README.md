# Orbit-Physics-Planet-Sim-in-C
it uses SDL and coded in just C imma mess up c# and c++

to run it you can do ./main and if that doesnt work do 

//gcc -o main main.c `sdl2-config --cflags --libs`
// then do ./main
